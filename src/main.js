import Vue from "vue";
import App from "./App.vue";
import "@/assets/css/tailwind.css";

import router from "@router";

Vue.config.productionTip = false;

new Vue({
  cuando el nombre de la propiedad es igual al nombre de la variable
  router,
  render: h => h(App)
}).$mount("#app");
